{
  "name": "newsletter_signup",
  "version": "1.0.0",
  "description": "News letter signup",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build": "npm-run-all --parallel build:*",
    "build:app": "some-build-tool app.js -o public/app.js",
    "build:images": "cp -R images/ public/"
  },
  "author": "Light",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "request": "^2.88.2"
  }
}
